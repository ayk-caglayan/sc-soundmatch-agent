=== EXAMPLE: scdoc_Classes_NLFiltL_ex1 ===

DESCRIPTION: A neutral pure_tone sound, with saw_like oscillator character around 1843 Hz, featuring punchy attack and compressed envelope, with chaotic spectral movement, filtered with steep_rolloff character, and spectrally simple.

CATEGORIES:
  attack_time: punchy
  brightness: neutral
  crest_factor_db: compressed
  envelope_flatness: flat
  harmonic_to_noise_ratio: pure_tone
  spectral_complexity_mean: simple
  spectral_flux_normalized: chaotic
  spectral_slope: steep_rolloff
  temporal_centroid: balanced

SYNTHESIS CONCEPTS (key values):
[oscillator]
  fundamental_freq: 1843.2408
  harmonic_decay_rate: -2.9652
  harmonic_regularity: 0.8064
  pitch_clarity: 0.0000
  inharmonicity: 0.0235
  type: saw_like
[filter]
  spectral_slope: -14.2178
  resonance_freq: 0.0000

SUPERCOLLIDER CODE:
// Title: NLFiltL Example 1
// Author: SC Documentation
// URL: http://doc.sccode.org/Classes/NLFiltL.html
// Tags: NLFiltL, Classes
// ---
// --- Example 1 ---
(
{
    NLFiltL.ar(
        LFSaw.ar([120, 180], 0, mul:0.1),
        0.5,
        -0.04,
        0.8,
        0.2,
        LFCub.kr(0.2, [0, 0.5 * pi], 63, 103)
    )
}.play
)

(
{
    NLFiltL.ar(
        LFSaw.ar(XLine.kr([60, 90], [360, 540], 20), 0, mul:0.1),
        0.0,
        0.0,
        0.8,
        0.4,
        LFCub.kr(0.2, [0, 0.5 * pi], 3, 9)
    )
}.play
)

(
// internal mirroring keeps it from blowing up and makes for some gross distortion
// this is a little loud
{
    NLFiltL.ar(
        LFPulse.ar([100, 150], mul:0.1),
        LFNoise2.kr(1).range(0.3, 0.5),
        0.2,
        0.7,
        0.4,
        LFCub.kr(0.2, [0, 0.5 * pi], 100, 400)
    )
}.play
)

(
{
    NLFiltL.ar(
        LFPulse.ar([100, 150], mul:0.1),
        0.7,
        -0.2,
        0.9,
        0.2,
        LFCub.kr(0.2, [0, 0.5 * pi], 100, 400)
    )
}.play
)

(
{
    NLFiltL.ar(
        LFPulse.ar([100, 150], mul:0.1),
        0.35,
        -0.3,
        0.95,
        0.2,
        LFCub.kr(0.2, [0, 0.5 * pi], 50, 100)
    )
}.play
)
